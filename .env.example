# Canonical site URL (no trailing slash). Used for OG tags, sitemap, robots, and llms.txt.
# Set both in production if you split build-time (Vite) and runtime (Nitro) env.
SITE_URL=https://example.com
VITE_SITE_URL=https://example.com
